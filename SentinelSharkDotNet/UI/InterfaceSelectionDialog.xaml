<Window x:Class="SentinelSharkDotNet.UI.InterfaceSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:SentinelSharkDotNet.UI.Controls"
        Title="SentinelShark - Select Network Interface"
        Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize"
        Background="Transparent">
    
    <Border Background="{DynamicResource CardBackgroundBrush}" 
            BorderBrush="{DynamicResource BorderBrush}" 
            BorderThickness="1" 
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Fill="{DynamicResource CriticalRedBrush}" Margin="0,0,8,0"/>
                    <Ellipse Width="12" Height="12" Fill="{DynamicResource WarningAmberBrush}" Margin="0,0,8,0"/>
                    <Ellipse Width="12" Height="12" Fill="{DynamicResource SafeGreenBrush}" Margin="0,0,16,0"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="SentinelShark - Select Network Interface" Foreground="{DynamicResource TextMutedBrush}" FontFamily="{DynamicResource UIFont}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>

            <!-- Header and Grid -->
            <StackPanel Grid.Row="1" Margin="24,0,24,0">
                <TextBlock Text="Select Network Interface for Packet Capture" Foreground="{DynamicResource AccentCyanBrush}" FontSize="16" FontWeight="Bold" FontFamily="{DynamicResource UIFont}" Margin="0,0,0,4"/>
                <TextBlock Text="Live traffic activity curves below show real-time packet transmission across your network devices (Wireshark-style preview)." Foreground="{DynamicResource TextMutedBrush}" FontSize="12" FontFamily="{DynamicResource UIFont}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                <DataGrid x:Name="InterfacesGrid" AutoGenerateColumns="False" HeadersVisibility="Column" 
                          CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                          GridLinesVisibility="None" Background="Transparent" RowBackground="Transparent"
                          AlternatingRowBackground="{DynamicResource PanelBackgroundBrush}"
                          BorderThickness="0" MouseDoubleClick="InterfacesGrid_MouseDoubleClick"
                          RowHeight="50" Foreground="{DynamicResource TextPrimaryBrush}">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentCyanBrush}"/>
                            <Setter Property="FontFamily" Value="{DynamicResource MonoFont}"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="8,4,8,4"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBlueDarkBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="INTERFACE" Binding="{Binding FriendlyName}" Width="2*"/>
                        <DataGridTextColumn Header="IP ADDRESS" Binding="{Binding IpAddress}" Width="1.5*"/>
                        <DataGridTemplateColumn Header="LIVE TRAFFIC SPARKLINES" Width="1.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <controls:SparklineControl x:Name="Sparkline" Margin="8,0" Loaded="Sparkline_Loaded" Tag="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="RATE" Binding="{Binding RateDisplay}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,24">
                <Button Content="Cancel" Click="CancelBtn_Click" Style="{DynamicResource ClearButtonStyle}" Width="120" Height="32" Margin="0,0,16,0"/>
                <Button Content="Start Capture on Interface" Click="StartBtn_Click" Style="{DynamicResource StartButtonStyle}" Width="200" Height="32"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
